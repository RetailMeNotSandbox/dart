import React from 'react';

const Header = () => {
    const style = {
        background: 'orange',
        height: '100px'
    };

    return (
        <div style={style}>
        </div>
    );
};

export default Header;
